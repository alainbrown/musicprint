services:
  core-test:
    image: gcc:12
    container_name: musicprint-core-test
    volumes:
      - ../:/workspace
    working_dir: /workspace/libmusicprint
    # Install cmake then run the Makefile test target
    command: >
      bash -c "apt-get update && apt-get install -y cmake && 
               make test"
