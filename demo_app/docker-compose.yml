services:
  demo:
    build:
      context: ..
      dockerfile: docker/Dockerfile.demo
    ports:
      - "5000:5000"
    volumes:
      - ../release:/app/release:ro
    environment:
      - RELEASE_DIR=/app/release
